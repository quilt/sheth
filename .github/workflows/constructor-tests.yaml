name: constructor-tests

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: 
        cargo build --manifest-path=constructor/Cargo.toml --release --verbose
    - name: Run tests
      run: 
        cargo test --manifest-path=constructor/Cargo.toml --release --verbose
